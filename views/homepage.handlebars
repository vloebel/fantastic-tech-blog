{{!-- 
  DISPLAYS each blog post
  CALLED BY
    controllers/home-routes.js
    res.render('homepage', {
        posts,
        loggedIn: req.session.loggedIn
      }); 
  CALLS
    post-info.post 
  --}}
<ul class="post-list">
  {{#each posts as |post|}}
  <li>
      {{> post-info post }}
  </li>
  {{/each}}
</ol>